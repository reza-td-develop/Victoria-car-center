export declare enum ImageIndicationType {
    error = "error",
    loader = "loader"
}
declare type StyledImageIndicationProps = {
    paddingBottom: number;
    custom?: React.ReactElement;
    type: ImageIndicationType;
};
declare const ImageIndication: ({ paddingBottom, custom, type, }: StyledImageIndicationProps) => JSX.Element;
export default ImageIndication;
